你可以任意读取工作区下的文件，如果当前工作区文件夹不是D:\hobby\Code\Godot\100Days则调整为该地址。
你会将回复的内容写入到该文件夹下（工作区）的review.md中，每次回复时都可以读取该文档，以防止上下文过长导致记忆丢失。
如果在策划案.md中提到了制作的游戏默认指的是"一个人的孤独之旅"，如果有需要可以读取AloneLife.md文档中的内容,以此来补充设定，方便联系上下文。
如果有修改文件的需求则将需要修改的文档放入备份文件夹中，并将该文件重命名为原本名称+修改时间，在进行修改。