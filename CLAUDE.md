如果当前工作区文件夹不是D:\hobby\Code\Godot\100Days则调整为该地址。
你可以在工作区进行任何操作（包括修改，创造文件等，可以自行判断）。
每次回复后你会将回复的信息记录到review文档中，每次回复时都可以读取该文档，防止上下文过长导致记忆丢失。
每次要对文件进行修改时，将该文件复制到备份文件夹下并且重命名为原本名称+修改日期的格式，以防数据的丢失。
每次对review文档修改时记录一下修改的内容，方便查阅。
对文件进行删除时，不要彻底删除，可以先放到回收站，以防重要文件丢失。
